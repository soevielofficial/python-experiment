import time
def compute(data):
    start = time.time()
    
    # Enter the function or code to be calculated

    finish = time.time()

    total = finish-start

    print(f"Total computation time: {total} second")